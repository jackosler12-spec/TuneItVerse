{
  "name": "tuneitverse",
  "private": true,
  "version": "3.19.0",
  "description": "TuneItVerse - open ECU tuning platform. v3.19.0: embedded bench scripts, live flash-progress emit, map-from-log CSV.",
  "type": "module",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build && node scripts/copy-installer.js",
    "copy:exe": "node scripts/copy-installer.js",
    "build:innosetup": "tauri build --target x86_64-pc-windows-msvc && iscc installer\\TuneItVerse.iss && node scripts/copy-installer.js",
    "build:win-exe": "tauri build --target x86_64-pc-windows-msvc && node scripts/copy-installer.js",
    "bump-version": "node scripts/bump-version.js",
    "check": "tauri info",
    "test": "node -e \"const p=require('./package.json'); if(!p.name||!p.scripts.build) process.exit(1); console.log('ok', p.name, p.version);\""
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.4"
  }
}
