# Disabled: TuneItVerse is a desktop Tauri app, not an npm package.
# Use GitHub Releases + tauri build / installer for distribution.
name: Node.js Package (disabled)

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Publishing to npm is disabled for this desktop app."
