{
  "name": "tuneitverse",
  "private": true,
  "version": "2.7.0",
  "description": "TuneItVerse - Industry-leading open ECU tuning platform (Tauri + Rust) - free alternative to expensive commercial tools. Fully operational DIY powerhouse for P01, EDC16, EDC17, MED17, P59. v2.7.0: aggressive full-repo analysis confirmed no critical gaps + continuous voltage + real Mode 01 + full logging + J2534 + DB-driven maps.",
  "type": "module",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build",
    "build:innosetup": "tauri build --target x86_64-pc-windows-msvc && iscc installer\\TuneItVerse.iss",
    "build:win-exe": "tauri build --target x86_64-pc-windows-msvc && node scripts/copy-installer.js",
    "bump-version": "node scripts/bump-version.js",
    "check": "tauri info",
    "test": "node -e \"const p=require('./package.json'); if(!p.name||!p.scripts.build) process.exit(1); console.log('ok', p.name, p.version);\""
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.4"
  }
}
