# Superseded by ci.yml — kept as a no-op redirect note so old badges don't confuse.
# Active workflow: .github/workflows/ci.yml
name: TuneItVerse (legacy)

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Use TuneItVerse CI workflow (ci.yml) instead."
